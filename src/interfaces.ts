
export interface IdObject {
    id: string;
};
export interface DataObject {
    "email" ?: string,
    "age" ?: number,
    "password" ?: string,
    "name" ?: string,
};

export type StrOrUnd = string | undefined;
